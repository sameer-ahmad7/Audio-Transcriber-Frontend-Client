import { NgxLetDirective } from './ngx-let.directive';

describe('NgxLetDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxLetDirective();
    expect(directive).toBeTruthy();
  });
});
